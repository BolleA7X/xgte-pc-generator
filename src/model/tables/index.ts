import * as origins from './origins'
import * as decisions from './decisions'
import * as events from './events'
import * as supplemental from './supplemental'

const tables = {
    origins: origins,
    decisions: decisions,
    events: events,
    supplemental: supplemental,
}

export default tables;